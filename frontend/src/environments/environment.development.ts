export const environment = {
  production: false,
  // Relative path, forwarded to http://localhost:8080 by proxy.conf.json
  // during `ng serve` - avoids CORS entirely without any backend change.
  apiBaseUrl: '/api'
};
